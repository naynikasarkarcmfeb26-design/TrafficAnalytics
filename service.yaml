apiVersion: v1
kind: Service

metadata:
  name: trafficanalytics-service

spec:
  selector:
    app: trafficanalytics

  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080

  type: NodePort